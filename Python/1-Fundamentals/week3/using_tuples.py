my_tuple = (1, 2, 3, 4, 5)
print(my_tuple[0] + my_tuple[1])

print(my_tuple[-1] + my_tuple[-2])

# my_tuple[3] = 1 doeesn't work tuples require less memory, but they're immutable, more efficient

my_tuple = (5, 4, 3, 2, 1)  # can overwrite previous tuple
print(my_tuple)
